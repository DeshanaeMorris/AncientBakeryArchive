<?xml version="1.0" encoding="UTF-8"?>

<?import java.lang.*?>
<?import java.util.*?>
<?import javafx.scene.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<?import javafx.scene.image.ImageView?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.image.Image?>

<AnchorPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.ancientbakery.ancientbakeryarchive.BakeController"
            stylesheets="@../styles.css"
            styleClass="era-screen"
            prefHeight="600.0" prefWidth="800.0">

    <!-- Left Sidebar: Era Navigation -->
    <VBox layoutY="60" layoutX="20" prefWidth="150" prefHeight="520"
          spacing="10" alignment="CENTER" styleClass="era-sidebar">

        <Button text="Ancient" styleClass="sidebar-link" onAction="#goAncient"/>
        <Button text="Medieval" styleClass="sidebar-link" onAction="#goMedieval"/>
        <Button text="Renaissance" styleClass="sidebar-link" onAction="#goRenaissance"/>
        <Button text="Industrial" styleClass="sidebar-link" onAction="#goIndustrial"/>
        <Button text="20th Century" styleClass="sidebar-link" onAction="#go20thCent"/>
        <Button text="Modern" styleClass="sidebar-link" onAction="#goModern"/>

        <Button text="BAKE NOW" styleClass="bake-now" prefHeight="50" prefWidth="120" onAction="#goBakeNow"/>

    </VBox>

    <!-- Tabs over split content: recipe parchment + conversions panel -->
    <TabPane layoutX="185" layoutY="20" prefWidth="595" prefHeight="560"
             tabClosingPolicy="UNAVAILABLE" styleClass="era-tabs">

        <Tab text="RECIPES">
            <HBox>
                <!-- Recipe parchment (left, wider) -->
                <VBox styleClass="parchment-ancient" alignment="TOP_CENTER" spacing="20"
                      prefWidth="395" HBox.hgrow="ALWAYS">
                    <padding><Insets top="35"/></padding>
                    <ImageView fitWidth="320" preserveRatio="true">
                        <Image url="@../images/RandAncient.png"/>
                    </ImageView>
                </VBox>

                <!-- Conversions panel (right, scrollable) -->
                <ScrollPane prefWidth="200" fitToWidth="true" styleClass="conversions-scroll">
                    <VBox alignment="TOP_CENTER" spacing="15" styleClass="conversions-panel">
                        <padding><Insets top="25" right="15" bottom="25" left="15"/></padding>
                        <Label text="Updated Conversions" wrapText="true"
                               styleClass="conversions-title"/>
                        <!-- conversion entries will go here -->
                    </VBox>
                </ScrollPane>
            </HBox>
        </Tab>

        <Tab text="PICTURES">
            <StackPane styleClass="parchment-ancient">
                <Label text="Pictures coming soon" styleClass="recipe-title"/>
            </StackPane>
        </Tab>

        <Tab text="HISTORY">
            <StackPane styleClass="parchment-ancient">
                <Label text="History coming soon" styleClass="recipe-title"/>
            </StackPane>
        </Tab>

        <Tab text="POPULAR">
            <StackPane styleClass="parchment-ancient">
                <Label text="Popular coming soon" styleClass="recipe-title"/>
            </StackPane>
        </Tab>
    </TabPane>
</AnchorPane>
